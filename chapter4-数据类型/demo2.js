
/*
var box = 250;  //十进制整型
alert(box);

var box = 070;   //八进制，按照十进制输出是56
alert(box);

var box = 0x1f;  //十六进制，
alert(box)

var box = 3.8;  //浮点型
alert(box)

var box = 8.0;  //自动转换成整型
alert(box)

var box = 4.12e8;  //科学计数法
alert(box)

var box = 0.00000000000412;  //自动转换为科学计数法
alert(box)

alert(Number.MIN_VALUE); //最小值
alert(Number.MAX_VALUE); //最大值

var box = -100e1000;  //超过范围
alert(box)

alert(Number.POSITIVE_INFINITY); //正无穷
alert(Number.NEGATIVE_INFINITY); //负无穷

var box = 0 / 0;  //数学中第一个可以为0，第二个不可以为0  NaN
alert(box);

alert(Number.NaN); //NaN

alert(NaN == NaN);  //不相等

var box = 0 / 0;
alert(isNaN(box));  //true

alert(Number(true)); //1
alert(Number(null)); //0
alert(Number('')); //0
alert(Number('eeee')); //NaN

parseInt(); //只能转换字符串至数值
parseFloat();  //只能转换字符串至浮点数值
 */

