
/*

var box = 3 > 2;
alert(box);   //关系运算符大多返回的是一个布尔值

var box = '3' > 2;
alert(box);   //如果只要一个数值字符串，那么会将它转换成数值，再比较

var box = '3' > '22';
alert(box);   //如果两个都是数值字符串，那么会进行第一个字符的比较，3>2

var box = 2 > {  //和对象比较
    toString:function () {
        return 3
    }
    };

var box = 'a'>'B'; //97>66
alert(box);
*/

